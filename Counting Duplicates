// Count the number of Duplicates

// Write a function that will return the count of distinct case-insensitive alphabetic characters and numeric digits that occur more than once in the input string. The input string can be assumed to contain only alphabets (both uppercase and lowercase) and numeric digits.
// Example

// "abcde" -> 0 # no characters repeats more than once
// "aabbcde" -> 2 # 'a' and 'b'
// "aabBcde" -> 2 # 'a' occurs twice and 'b' twice (`b` and `B`)
// "indivisibility" -> 1 # 'i' occurs six times
// "Indivisibilities" -> 2 # 'i' occurs seven times and 's' occurs twice
// "aA11" -> 2 # 'a' and '1'
// "ABBA" -> 2 # 'A' and 'B' each occur twice



package kata

import (
   "fmt"
   "strings"
)

func duplicate_count(s1 string) int {
    
    var c, i int
    c1 := 0
    for ind, val := range strings.ToLower(s1) {
      fmt.Println(string(val), c)
      c = strings.Count(strings.ToLower(s1), string(val))
      i = strings.Index(strings.ToLower(s1), string(val))
      if c >= 2 && ind == i {
        c1++
      }
      
    }
    
	  fmt.Println(c1)
  
    return c1 //Instead of returning '1', you have to return integer 'i' as answer of solution.  
    }


